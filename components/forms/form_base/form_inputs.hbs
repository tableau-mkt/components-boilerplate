{{#with form }}
  {{#each fields}}
  <div class="form__field">
    <label class="form__field__label" for="{{name}}">
      {{label}}
    </label>

    <input type="{{type}}" name="{{name}}" id="text-field"/>
  </div>
  {{/each}}
{{/with }}
